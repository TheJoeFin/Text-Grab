<Page
    x:Class="Text_Grab.Pages.VoiceOutputSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Text_Grab.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="VoiceOutputSettings"
    d:DesignHeight="700"
    d:DesignWidth="800"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Visible">
        <StackPanel Margin="20,12,24,40" Orientation="Vertical">
            <TextBlock Style="{StaticResource SettingsPageTitle}" Text="Voice Output Settings" />

            <TextBlock Style="{StaticResource SettingsSectionHeader}" Text="Voice" />

            <controls:SettingsCard
                Description="Choose from the voices installed on this device."
                HeaderText="Voice"
                Icon="{ui:SymbolIcon PersonVoice24}">
                <ComboBox
                    x:Name="VoiceComboBox"
                    Width="260"
                    SelectionChanged="VoiceComboBox_SelectionChanged" />
            </controls:SettingsCard>

            <controls:SettingsCard
                Description="How quickly the text is spoken. 1.0 is the default speed."
                HeaderText="Speaking rate"
                Icon="{ui:SymbolIcon TopSpeed24}">
                <StackPanel Orientation="Horizontal">
                    <Slider
                        x:Name="SpeakingRateSlider"
                        Width="200"
                        VerticalAlignment="Center"
                        IsSnapToTickEnabled="True"
                        Maximum="4.0"
                        Minimum="0.5"
                        TickFrequency="0.1"
                        ValueChanged="SpeakingRateSlider_ValueChanged" />
                    <TextBlock
                        x:Name="SpeakingRateValue"
                        Width="32"
                        Margin="12,0,0,0"
                        VerticalAlignment="Center" />
                </StackPanel>
            </controls:SettingsCard>

            <controls:SettingsCard
                Description="Stop speaking after this many words. Set to 0 for no limit."
                HeaderText="Word limit"
                Icon="{ui:SymbolIcon NumberSymbol24}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        x:Name="TtsWordLimitError"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                        Text="⚠ Must be a positive number"
                        Visibility="Collapsed" />
                    <TextBox
                        x:Name="TtsSpeakWordLimitTextBox"
                        Width="60"
                        Height="26"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Background="White"
                        FontWeight="Medium"
                        Foreground="Black"
                        Style="{StaticResource TextBoxStyle1}"
                        TextChanged="TtsSpeakWordLimitTextBox_TextChanged" />
                </StackPanel>
            </controls:SettingsCard>

            <controls:SettingsCard
                Description="Speak a short announcement when a slow operation (such as an AI image description) is still processing."
                HeaderText="Announce when a slow operation is processing"
                Icon="{ui:SymbolIcon ReadAloud24}">
                <ui:ToggleSwitch
                    x:Name="SpeakProcessingStatusCheckBox"
                    Checked="SpeakProcessingStatusCheckBox_CheckChanged"
                    Unchecked="SpeakProcessingStatusCheckBox_CheckChanged" />
            </controls:SettingsCard>

            <controls:SettingsCard
                Description="Hear a sample sentence with the current voice and speaking rate."
                HeaderText="Preview"
                Icon="{ui:SymbolIcon Play24}">
                <Button
                    x:Name="PreviewVoiceButton"
                    Click="PreviewVoiceButton_Click"
                    Content="Speak sample" />
            </controls:SettingsCard>

        </StackPanel>
    </ScrollViewer>
</Page>
