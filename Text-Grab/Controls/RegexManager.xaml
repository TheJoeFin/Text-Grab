<ui:FluentWindow
    x:Class="Text_Grab.Controls.RegexManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Text_Grab.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Patterns Manager"
    AutomationProperties.AutomationId="RegexManagerDialog"
    Width="900"
    Height="600"
    MinWidth="700"
    MinHeight="450"
    Closing="FluentWindow_Closing"
    Loaded="Window_Loaded"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <ui:ImageIcon x:Key="TextGrabIcon" Source="/Text-Grab;component/Images/TealSelect40.png" />
    </Window.Resources>

    <Grid Background="{ui:ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="Patterns Manager"
            Height="32"
            Margin="2,2,2,0"
            Padding="8,2"
            Icon="{StaticResource TextGrabIcon}" />

        <!--  Main DataGrid  -->
        <Border
            Grid.Row="1"
            Margin="6"
            Background="{ui:ThemeResource SolidBackgroundFillColorBaseAltBrush}">
            <DataGrid
                x:Name="RegexDataGrid"
                AutomationProperties.AutomationId="RegexManager.PatternsGrid"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CellStyle="{DynamicResource DataGridCellStyleDark}"
                ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyleDark}"
                HeadersVisibility="Column"
                HorizontalScrollBarVisibility="Disabled"
                IsReadOnly="True"
                SelectionChanged="RegexDataGrid_SelectionChanged"
                SelectionMode="Single"
                SelectionUnit="FullRow">
                <DataGrid.RowStyle>
                    <Style BasedOn="{StaticResource DataGridRowStyleDark}" TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                <Setter Property="Opacity" Value="0.5" />
                                <Setter Property="FontStyle" Value="Italic" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="auto"
                        Binding="{Binding Name}"
                        Header="Name" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding PatternDisplay}"
                        Header="Pattern" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Description}"
                        Header="Description" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!--  Action Buttons  -->
        <StackPanel
            Grid.Row="2"
            Margin="6,0,6,6"
            Orientation="Horizontal">
            <ToggleButton
                x:Name="ShowTestToggle"
                AutomationProperties.AutomationId="RegexManager.ShowTestToggle"
                Margin="0,0,6,0"
                Padding="10,6"
                Click="ShowTestToggle_Click"
                Content="Show Test"
                IsChecked="False"
                Style="{DynamicResource ToggleSymbolButton}" />
            <ui:Button
                x:Name="AddButton"
                AutomationProperties.AutomationId="RegexManager.AddButton"
                Margin="0,0,6,0"
                Click="AddButton_Click"
                Content="Add New"
                Icon="{ui:SymbolIcon Add24}" />
            <ui:Button
                x:Name="EditButton"
                AutomationProperties.AutomationId="RegexManager.EditButton"
                Margin="0,0,6,0"
                Click="EditButton_Click"
                Content="Edit"
                Icon="{ui:SymbolIcon Edit24}"
                IsEnabled="False" />
            <ui:Button
                x:Name="DeleteButton"
                AutomationProperties.AutomationId="RegexManager.DeleteButton"
                Margin="0,0,6,0"
                Appearance="Danger"
                Click="DeleteButton_Click"
                Content="Delete"
                Icon="{ui:SymbolIcon Delete24}"
                IsEnabled="False" />
            <ui:Button
                x:Name="HideButton"
                Margin="0,0,6,0"
                Click="HideButton_Click"
                Content="Hide"
                Icon="{ui:SymbolIcon EyeOff24}"
                IsEnabled="False"
                Visibility="Collapsed" />
            <ui:Button
                x:Name="ExplainButton"
                AutomationProperties.AutomationId="RegexManager.ExplainButton"
                Margin="0,0,6,0"
                Click="ExplainButton_Click"
                Content="Explain"
                Icon="{ui:SymbolIcon Info24}"
                IsEnabled="False" />
            <ui:Button
                x:Name="UseButton"
                AutomationProperties.AutomationId="RegexManager.UseButton"
                Margin="0,0,6,0"
                Appearance="Primary"
                Click="UseButton_Click"
                Content="Use in Find &amp; Replace"
                Icon="{ui:SymbolIcon Search24}"
                IsEnabled="False" />
        </StackPanel>

        <Border
            x:Name="TestPanel"
            AutomationProperties.AutomationId="RegexManager.TestPanel"
            Grid.Row="3"
            Margin="6"
            Background="{ui:ThemeResource SolidBackgroundFillColorBaseAltBrush}"
            Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="6"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Text="Test Pattern" />

                <Grid Grid.Row="1">
                    <TextBox
                        x:Name="TestTextBox"
                        AutomationProperties.AutomationId="RegexManager.TestText"
                        MinHeight="160"
                        Margin="6,0,6,6"
                        AcceptsReturn="True"
                        TextChanged="TestTextBox_TextChanged"
                        TextWrapping="Wrap"
                        VerticalScrollBarVisibility="Auto" />
                    <Label
                        x:Name="TestTextPlaceholder"
                        Margin="12,6,6,6"
                        Padding="4"
                        VerticalAlignment="Top"
                        Content="Enter test text here..."
                        FontSize="14"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        IsHitTestVisible="False"
                        Opacity="0.5" />
                </Grid>

                <StackPanel
                    Grid.Row="2"
                    Margin="6,0,6,6"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        Text="Matches:" />
                    <TextBlock
                        x:Name="MatchCountText"
                        AutomationProperties.AutomationId="RegexManager.MatchCount"
                        VerticalAlignment="Center"
                        FontWeight="Bold"
                        Text="0" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
